# API Migration Evaluation — pre vs post change

This repository contains two projects demonstrating an API migration from /api/v1/checkStock to /api/v2/stock/availability.

Top-level commands:

- Run both projects and produce a comparison report:

```bash
./run_all.sh
```

Each project folder has its own run_tests.sh to run the tests and produce results in `results/`.
